# Intellij-java-record-Plugin

This plugin helps to convert java classes to records automatically.
It is available via contextual menu inside the editor, or on the project files tree in order 
to be applied recursively on the underlying java classes. 
